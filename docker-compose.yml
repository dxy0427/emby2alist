services:
  emby2alist:
    build: .
    image: emby2alist:latest
    container_name: emby2alist
    restart: unless-stopped
    network_mode: host
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./config.yaml:/app/config.yaml